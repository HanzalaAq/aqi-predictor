name: Inference Pipeline

on:
  schedule:
    - cron: '30 * * * *'  # Runs every hour at 30 minutes past
  workflow_dispatch:  # Allows manual trigger

jobs:
  generate-predictions:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Cache pip packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run inference pipeline
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
          MONGODB_DATABASE: aqi_features
          MODEL_DATABASE: aqi_models
          PREDICTION_DATABASE: aqi_predictions
          LATITUDE: 24.8607
          LONGITUDE: 67.0011
          CITY_NAME: Karachi
        run: |
          python src/pipelines/inference_pipeline.py
      
      - name: Pipeline completion message
        if: success()
        run: echo "✅ Inference pipeline completed successfully!"
      
      - name: Pipeline failure message
        if: failure()
        run: echo "❌ Inference pipeline failed!"